import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lifeboat.www',
  appName: 'Lifeboat',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
